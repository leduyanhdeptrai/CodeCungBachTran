#include <bits/stdc++.h>

using namespace std;
int n, t;
string s;
int main()
{
    cin >> t;
    while(t --){
      cin >> s;
      n = s.length();
      if(n <= 10) cout << s;
      else cout << s[0] << n-2 << s[n-1];
      cout << '\n';
    }
    return 0;
}
