#include <bits/stdc++.h>

using namespace std;

int main()
{
    string a, b, c;
    cin >> a >> b;
    int lena = a.length(), lenb = b.length();
    if(lena < lenb) swap(a,b), swap(lena,lenb);
    for(int i = 1; i <= lena - lenb; ++i) b = "0" + b;
    int nho = 0;
    c = a;
    for(int i = lena-1; i >= 0; --i){
      if(a[i] + b[i]-'0'-'0' + nho >= 10) c[i] = (a[i] + b[i] - '0'*2 + nho) % 10 + '0', nho=1;
      else c[i] = a[i] + b[i]- '0' + nho, nho = 0;
    }
    if(nho) cout << 1 << c;
    else cout << c;
    return 0;
}
